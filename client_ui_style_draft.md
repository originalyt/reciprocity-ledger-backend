# 礼账 App 客户端 UI 风格草案

## 1. 风格定位

### 1.1 关键词
- 简约
- 清晰
- 克制
- 轻量
- 可信

### 1.2 整体气质
- 产品风格定位为偏工具型，而不是社交型或强财务后台型。
- 页面应优先传达“信息清楚、层级明确、操作直接”的感受。
- 不追求花哨视觉效果，强调长期使用时的舒适度与耐看性。

### 1.3 视觉方向
- 浅色背景
- 低饱和主色
- 明确的信息层级
- 轻卡片结构
- 适度留白

## 2. 设计原则

- 先让用户看懂，再追求视觉好看。
- 金额、状态、联系人是最重要的信息层级。
- 减少装饰性元素，避免页面视觉噪声。
- 一个页面只突出一个重点任务。
- 颜色主要用于表达状态、操作重点和少量视觉引导。

## 3. 配色草案

### 3.1 基础配色
- 主色：`#B35C44`（豆沙砖红）
- 主色浅背景：`#F6EAE5`
- 页面背景：`#F7F7F5`
- 卡片背景：`#FFFFFF`
- 主文字：`#222222`
- 次级文字：`#666666`
- 弱文字：`#999999`
- 分割线：`#E8E6E3`

### 3.2 状态色
- 待我回礼：`#E59A2F`
- 待对方回礼：`#4C84D3`
- 已互回：`#5E9B6B`
- 无需回礼：`#9A9A9A`
- 作废/失效：`#C2C2C2`

### 3.3 配色使用规则
- 状态色优先用于标签、图标点缀、少量数字提示。
- 避免使用大面积高饱和纯色。
- 主色用于主按钮、选中态、关键入口，不宜过度泛滥。
- 页面整体以浅底和中性色为主，保持干净通透。

## 4. 字体与排版

### 4.1 字号层级建议
- `32`：首页核心金额
- `24`：模块主标题 / 重点数字
- `20`：页面标题
- `16`：列表主信息、按钮文字
- `14`：正文、次级说明
- `12`：标签、辅助说明、日期信息

### 4.2 字重建议
- 金额：`SemiBold` 或更高字重
- 联系人名 / 事件名：中高字重
- 日期、备注、关系：常规字重

### 4.3 排版原则
- 金额视觉权重高于辅助信息。
- 联系人名、事件名应优先于关系、备注显示。
- 列表信息顺序保持稳定，避免不同页面排列方式不一致。
- 建议使用等宽数字或视觉等宽数字，提升金额与日期对齐感。

## 5. 间距、圆角与阴影

### 5.1 间距体系
以 `8` 为基础单位：
- `4`：微间距
- `8`：组件内边距
- `12`：小模块间距
- `16`：常规卡片内边距
- `20`：页面模块间距
- 页面左右主边距建议为 `16`

### 5.2 圆角体系
- 输入框：`12`
- 按钮：`12`
- 标签：胶囊圆角
- 卡片：`16`
- 弹层：`20`

### 5.3 阴影建议
- 卡片使用极轻阴影或直接无阴影。
- 优先依靠留白、底色层次、分组关系区分模块。
- 避免厚重阴影导致页面显得浮夸。

## 6. 组件样式草案

### 6.1 主按钮
- 背景色：主色 `#B35C44`
- 文字颜色：白色
- 圆角：`12`
- 高度建议：`48~52`
- 风格要求：稳重、简洁、可点击性清晰

### 6.2 次按钮
- 背景色：白色
- 边框：`#E8E6E3`
- 文字颜色：主文字色
- 用于次级操作，如取消、筛选、更多操作

### 6.3 状态标签
- 使用低饱和浅底 + 深色文字形式
- 标签文字应短小、明确、可快速扫读
- 各状态标签风格应统一，不同状态仅改变颜色，不改变结构

### 6.4 输入框
- 建议使用浅灰底或白底输入框
- 默认边框弱化，聚焦时再突出主色细边
- 错误态使用细红边或提示文字，不做强烈大面积红色警告
- 表单设计强调清爽、整齐、录入效率

### 6.5 卡片
- 白色背景
- 圆角 `16`
- 内边距 `16`
- 模块信息优先通过留白分组，而不是通过大量分割线切割

### 6.6 筛选条
- 采用胶囊按钮或 Chip 风格
- 选中态：主色浅底 + 主色文字
- 未选中态：白底 + 灰边 + 深灰字
- 保持所有筛选组件样式统一

## 7. 核心页面风格建议

### 7.1 首页
风格目标：温和、整洁、可信。

建议结构：
- 顶部标题区
- 三大指标卡区：总随礼、总收礼、净额
- 待办提醒区：我待回礼、无需回礼
- 快捷入口区：新增随礼、新增收礼、联系人、回礼清单
- 近期内容区：Top 联系人或最近记录

设计要求：
- 首屏信息不要堆得太满。
- 首页不要做成复杂 dashboard。
- 每个模块承担明确职责，不做过多视觉争抢。

### 7.2 新增/编辑记录页
风格目标：高效、低干扰、清楚。

建议结构：
- 联系人选择
- 记录类型选择
- 事件类型选择
- 事件说明输入
- 日期选择
- 金额输入
- 备注输入
- 底部提交按钮

设计要求：
- 表单区保持单列、稳定间距。
- 支持“最近联系人”“常用事件”快捷选择区。
- 提交按钮建议固定底部，减少滚动后找不到操作入口的问题。

### 7.3 时间线页
风格目标：层次明确、便于扫描。

列表项建议结构：
- 第一行：联系人 + 状态/类型标签
- 第二行：事件类型 + 事件说明
- 第三行：金额 + 日期

设计要求：
- 信息顺序在所有卡片中保持一致。
- 金额明显，但不要压过联系人与事件信息。
- 筛选条保持固定风格，便于频繁使用。

### 7.4 回礼清单页
风格目标：状态清晰、便于管理。

建议结构：
- 顶部 Tab：我待回礼 / 待对方回礼 / 已互回 / 无需回礼
- 顶部筛选条：时间范围、关系、排序方式
- 列表内容：联系人、事件、状态标签、金额差额、最近日期

设计要求：
- `无需回礼` 与真实待办需明显区分。
- 状态标签颜色统一但辨识度高。
- 相关操作可进入详情页或放在轻操作菜单中。

### 7.5 联系人详情页
风格目标：有温度，但不过度装饰。

建议结构：
- 联系人基础信息卡
- 汇总信息区：总随礼、总收礼、净额、最近往来
- 该联系人时间线列表

设计要求：
- 信息块要整洁，避免过多装饰线条。
- 详情页可比列表页稍柔和，但整体仍保持克制。

## 8. 图标、插画与空状态

### 8.1 图标
- 建议统一使用线性图标。
- 保持统一笔画粗细与风格。
- 不混用线性、拟物、重填充等多种图标体系。

### 8.2 插画
- 插画仅建议用于空状态页。
- 风格应轻量、简洁，不喧宾夺主。
- 避免大量使用装饰性插画影响产品克制感。

### 8.3 空状态文案示例
- 还没有礼账记录
- 还没有待回礼事项
- 没找到匹配联系人

要求：
- 文案短、直接、无情绪负担。
- 可配合简单线性插图或图标。

## 9. 动效建议

- 页面切换保持轻量顺滑。
- 筛选切换、状态变化、提交成功适合短反馈动效。
- “设为无需回礼”或“取消无需回礼”建议有明确但克制的反馈动效。
- 避免炫技型长动画，影响工具型产品效率感。

动效关键词：
- 短
- 轻
- 稳

## 10. Material 3 落地建议

结合当前技术选型（Flutter + Material 3），建议：
- `ColorScheme` 以暖白底 + 豆沙主色为核心
- 卡片降低 `elevation`
- 按钮优先使用圆角 Filled Button / Tonal Button
- 筛选建议使用 Chips
- 表单优先使用 Filled Text Field，弱化厚重边框

## 11. 不建议采用的风格方向

- 过度中国红婚庆风
- 强金融看盘风
- 过度可爱手账风
- 过度商务后台风

原因：
- 会削弱“简约清晰”的核心目标。
- 与礼账产品长期使用场景不够匹配。
- 容易导致视觉疲劳或产品气质跑偏。

## 12. 总结

本项目客户端视觉风格建议总结如下：
- 背景：暖白、浅灰为主
- 主色：豆沙砖红
- 结构：轻卡片、大留白、层级清楚
- 信息重点：金额、状态、联系人
- 整体气质：简约清晰，带一点温度

后续如进入页面级设计，建议优先输出以下三张核心页面的低保真或中保真视觉稿：
- 首页
- 新增/编辑记录页
- 回礼清单页
